% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_functions.R
\name{record_call}
\alias{record_call}
\title{Record a call in a staged event tree}
\usage{
record_call(object, call)
}
\arguments{
\item{object}{an object of class \code{sevt}.}

\item{call}{the call to record, normally \code{match.call()} evaluated
in the function which is recording it.}
}
\value{
\code{object} with \code{call} and \code{calls} updated.
}
\description{
Store the call which produced \code{object} in its \code{call} field,
and append it to the \code{calls} field.
}
\details{
Only the functions which build a staging or rebuild one record
a call, so \code{calls} is the list of those, and not a
history of everything an object has been through: fitting,
replacing stages directly, or taking a subtree leave no trace
in it.
}
\keyword{internal}
