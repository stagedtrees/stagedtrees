% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_lp_fast}
\alias{predict_lp_fast}
\title{Class-conditional log-probabilities for fully observed rows}
\usage{
predict_lp_fast(object, newdata, class, vars)
}
\arguments{
\item{object}{an object of class \code{sevt} with fitted probabilities.}

\item{newdata}{rows with no missing value among the predictors.}

\item{class}{character, name of the variable being predicted.}

\item{vars}{the model's variable names, in tree order.}
}
\value{
a matrix with one row per observation and one column per level of
\code{class}, holding normalised log-probabilities.
}
\description{
Internal helper for \code{\link{predict.sevt}}.
}
\details{
Marshals the model into the flat arrays the compiled kernel walks:
level codes in tree order, the level count per variable, the
situation-to-stage map per variable, and a stage-by-level
probability matrix per variable. The kernel carries the situation
index down each path exactly as \code{\link{path_probability}}
does, for every row and every candidate class level in one call.
}
\keyword{internal}
